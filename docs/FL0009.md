# FL0009: Prefer `""` over `string.Empty`

`""` and `string.Empty` are essentially the same, and there are no known cases in which the choice of one over the other would signal anything important to a reader.

Since the choice is entirely arbitrary, we arbitrarily choose `""` as the preference that should be used everywhere.
