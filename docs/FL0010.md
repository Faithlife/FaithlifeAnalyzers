# FL0010: Prefer modern language features over `IfNotNull`

`IfNotNull` was originally built because there was no `?.` operator in C#. Now that this operator _does_ exist, `IfNotNull` usage should be phased out.
