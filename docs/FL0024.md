# FL0024: Lambda operators should end the previous line

The lambda operator (`=>`) should end the previous line instead of beginning the next line.

```csharp
// discouraged
public int GetValue()
    => 1;

// preferred
public int GetValue() =>
    1;
```

This style applies to lambda expressions, expression-bodied members, and switch expressions.
